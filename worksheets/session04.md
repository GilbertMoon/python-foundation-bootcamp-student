# Session 04 Worksheet — 현실 데이터를 구조화하고 여러 조건 함께 다루기

> **수업일:** 2026-09-17  
> **대상 Chapter:** Chapter 07~08  
> **수업 시간:** 120분

이 워크시트는 정답지가 아닙니다.

오늘은 현실의 대상을 Python 안에서 **의미가 보이는 데이터 구조로 표현하고**, 여러 조건을 한꺼번에 다루는 연습을 합니다.

오늘의 핵심 흐름은 다음과 같습니다.

```text
현실의 대상
→ 필요한 속성 찾기
→ Dictionary로 한 대상 표현
→ List로 여러 대상 묶기
→ 조건을 하나씩 분해
→ AND / OR 관계 확인
→ 결과 만들기
→ 결과를 데이터에 저장
```

---

## 이름

- 이름: ______________________________
- 오늘 날짜: _________________________

---

# 오늘의 수업 규칙

오늘도 코드보다 **데이터와 조건의 구조**를 먼저 생각합니다.

```text
1. 현실의 대상이 무엇인지 찾는다.
2. 문제 해결에 필요한 속성만 고른다.
3. 한 대상을 Dictionary로 설계한다.
4. 같은 구조의 여러 대상을 List에 담는다.
5. 요구사항에서 조건을 하나씩 분리한다.
6. 조건 사이가 AND인지 OR인지 확인한다.
7. 실행 전에 경계값 결과를 예상한다.
8. Python으로 구현한다.
9. 결과를 출력만 할지 데이터에 저장할지 판단한다.
10. 마지막에 AI로 구조와 조건을 검증한다.
```

수업 초반에는 다음을 사용하지 않습니다.

- 생성형 AI
- 일반 웹 검색
- 블로그 예제 코드
- 다른 사람의 완성 코드

---

# Part 1. Memory Coding — 0~10분

다음 현실 정보를 보고 **List of Dictionaries** 구조를 아무 자료도 보지 않고 직접 만드세요.

```text
민수: 점수 85, 전공 AI, 출석률 92
지영: 점수 72, 전공 경영, 출석률 88
현우: 점수 91, 전공 통계, 출석률 97
서연: 점수 84, 전공 디자인, 출석률 75
```

조건:

```text
변수 이름은 students
학생 한 명은 Dictionary
여러 학생은 List
속성은 name, score, major, attendance
```

```python
# 아무 자료도 보지 않고 직접 작성하세요.













```

### 작성 후 체크

- [ ] 학생 한 명을 Dictionary로 표현했다.
- [ ] 각 속성에 의미 있는 key 이름을 사용했다.
- [ ] 같은 구조의 학생 여러 명을 하나의 List에 넣었다.
- [ ] 문자열과 숫자 값을 자연스럽게 구분했다.
- [ ] 특정 학생의 `score`, `attendance`를 key로 꺼낼 수 있다.

내가 기억하지 못했거나 헷갈렸던 부분:

```text


```

---

# Part 2. Explain — 10~20분

### 1. 왜 학생 한 명을 다음처럼 표현하는 것보다 Dictionary가 읽기 쉬울까요?

```python
["민수", 85, "AI", 92]
```

```text


```

### 2. `student["score"]`에서 `"score"`가 주는 의미는 무엇인가요?

```text


```

### 3. 학생이 100명이라면 왜 같은 구조를 유지하는 것이 중요할까요?

```text


```

### 4. 학생 한 명을 데이터베이스의 표로 생각하면 무엇과 비슷할까요?

```text


```

### 5. `name`, `score`, `major`, `attendance`는 표에서 무엇과 비슷할까요?

```text


```

---

# Part 3. Chapter 07 — 현실 데이터를 Python 구조로 표현하기 — 20~45분

## 문제 A. 주문 데이터를 직접 설계하기

쇼핑몰에 다음 주문이 들어왔습니다.

```text
주문 1001: 고객 민수, 금액 48,000원, 결제 완료
주문 1002: 고객 지영, 금액 72,000원, 결제 완료
주문 1003: 고객 현우, 금액 35,000원, 결제 미완료
주문 1004: 고객 서연, 금액 91,000원, 결제 완료
```

앞으로 다음 문제를 해결할 예정입니다.

```text
- 결제 완료 주문만 찾기
- 주문 금액으로 무료배송 여부 판단하기
- 총 주문 금액 계산하기
```

**아직 Python 코드를 작성하지 마세요.**

먼저 필요한 속성을 설계합니다.

| 속성 이름 후보 | 의미 | 자료형 예상 | 왜 필요한가? |
|---|---|---|---|
| | | | |
| | | | |
| | | | |
| | | | |
| | | | |

---

## 한 주문의 Dictionary 설계

주문 1001 한 건만 Python 데이터로 표현한다면 어떤 구조가 될까요?

```python
# 직접 설계하세요.





```

### 질문

```text
주문번호는 왜 필요한가?
고객명은 왜 필요한가?
amount는 숫자로 두는 것이 왜 좋은가?
paid는 어떤 자료형이 자연스러운가?
```

내 답:

```text


```

---

## 여러 주문 구조로 확장하기

한 주문 구조가 정해졌다면 여러 주문은 어떤 구조가 자연스러울까요?

```text
내가 선택한 구조:

이유:
```

이제 네 주문을 직접 Python으로 표현합니다.

```python
# orders 데이터를 직접 작성하세요.











```

---

## 구조를 표로 바꾸어 생각하기

내가 만든 주문 데이터를 표라고 생각해 봅니다.

| Python 구조 | 표/DB에서 비슷한 개념 |
|---|---|
| 주문 한 건의 Dictionary | |
| `order_id` | |
| `customer` | |
| `amount` | |
| `paid` | |
| 여러 주문을 담은 List | |

### 핵심 질문

> 데이터를 잘 표현하면 이후의 조건, 반복, 필터링, 집계가 왜 쉬워질까요?

```text


```

---

# Part 4. Predict & Run — 45~55분

내가 만든 `orders` 구조를 사용해 각 주문의 정보를 출력합니다.

실행 전에 먼저 예상합니다.

```text
orders의 길이 예상: ____________________
첫 번째 주문번호 예상: _________________
세 번째 주문의 paid 예상: ______________
네 번째 주문의 amount 예상: ____________
```

직접 구현:

```python
# 앞에서 만든 orders를 사용하세요.
# 주문을 하나씩 확인해서 주문번호, 고객, 금액, paid를 출력합니다.







```

실행 후 확인:

| 확인 항목 | 내 확인 결과 |
|---|---|
| 네 주문이 모두 출력되었는가? | |
| key 이름이 모든 주문에서 일관적인가? | |
| amount가 숫자로 저장되어 있는가? | |
| paid가 True/False로 자연스럽게 표현되었는가? | |

---

# 5분 Checkpoint — 55~60분

다음 문장을 완성합니다.

> 좋은 데이터 구조는 현실의 대상을 ____________________________________________.

> 한 대상은 ____________________, 같은 구조의 여러 대상은 ____________________로 표현할 수 있다.

---

# Part 5. Chapter 08 — 여러 조건과 여러 데이터를 함께 다루기 — 60~80분

이번에는 학생 데이터를 사용합니다.

```python
students = [
    {"name": "민수", "score": 85, "attendance": 92},
    {"name": "지영", "score": 72, "attendance": 88},
    {"name": "현우", "score": 91, "attendance": 97},
    {"name": "서연", "score": 84, "attendance": 75}
]
```

요구사항:

```text
1. 출석률이 80 이상이면서 점수가 80 이상이면 "수료"이다.
2. 두 조건 중 하나라도 만족하지 못하면 "보완 필요"이다.
3. 점수가 90 이상이면 level을 "우수"로 표시한다.
4. 그 외에는 level을 "일반"으로 표시한다.
5. status와 level을 각 학생 Dictionary에 새 속성으로 저장한다.
```

---

## 요구사항을 조건으로 분해하기

| 항목 | 내 분석 |
|---|---|
| 사용할 데이터 | |
| 조건 1 | |
| 조건 2 | |
| 조건 1과 2의 관계 | AND / OR |
| status 결과 | |
| 추가 조건 | |
| level 결과 | |
| 결과를 어디에 저장할 것인가? | |

---

## AND를 말로 설명하기

`출석률 >= 80 AND 점수 >= 80`의 의미를 Python 문법 없이 설명하세요.

```text


```

다음 중 수료가 되려면 어느 조건을 만족해야 할까요?

```text
출석만 통과
점수만 통과
둘 다 통과
둘 중 하나만 통과해도 됨
```

내 답: ______________________________

이유:

```text


```

---

## 경계값을 직접 판단하기

| 출석률 | 점수 | status 예상 | 이유 |
|---:|---:|---|---|
| 80 | 80 | | |
| 79 | 90 | | |
| 90 | 79 | | |
| 80 | 90 | | |
| 100 | 100 | | |

### 질문

`80 이상`에서 정확히 80은 포함될까요?

```text


```

---

# Part 6. Main Practice — 80~105분

## 문제 B. 학생 데이터를 가공해서 새로운 정보 저장하기

다음 데이터를 사용합니다.

```python
students = [
    {"name": "민수", "score": 85, "attendance": 92},
    {"name": "지영", "score": 72, "attendance": 88},
    {"name": "현우", "score": 91, "attendance": 97},
    {"name": "서연", "score": 84, "attendance": 75}
]
```

요구사항:

```text
- 모든 학생을 하나씩 처리한다.
- 출석률 80 이상 AND 점수 80 이상이면 status = "수료"
- 그렇지 않으면 status = "보완 필요"
- 점수 90 이상이면 level = "우수"
- 그 외에는 level = "일반"
- 계산한 status와 level을 기존 학생 Dictionary에 저장한다.
- 마지막에 이름, 점수, 출석률, status, level을 출력한다.
```

---

## Step 1. Think Before Code

| 질문 | 내 답 |
|---|---|
| 반복 대상은 무엇인가? | |
| 한 번의 반복에서 처리하는 대상은? | |
| 꺼내야 하는 값은 무엇인가? | |
| status를 결정하는 조건은 몇 개인가? | |
| 두 조건의 관계는? | |
| level을 결정하는 조건은? | |
| 새로 저장할 key는 무엇인가? | |
| 출력 전에 데이터에 어떤 변화가 생기는가? | |

---

## Step 2. 실행 전 결과 예상

| 학생 | 점수 | 출석률 | status 예상 | level 예상 |
|---|---:|---:|---|---|
| 민수 | 85 | 92 | | |
| 지영 | 72 | 88 | | |
| 현우 | 91 | 97 | | |
| 서연 | 84 | 75 | | |

---

## Step 3. 처리 순서를 자연어로 작성

```text
1.
2.
3.
4.
5.
6.
7.
8.
```

---

## Step 4. 구현

```python
students = [
    {"name": "민수", "score": 85, "attendance": 92},
    {"name": "지영", "score": 72, "attendance": 88},
    {"name": "현우", "score": 91, "attendance": 97},
    {"name": "서연", "score": 84, "attendance": 75}
]

# 여기에 직접 구현하세요.















```

---

## Step 5. 처리 후 데이터 구조 확인

코드를 실행한 뒤 `students` 자체를 출력해 봅니다.

내가 예상하는 학생 한 명의 구조:

```python
# 민수 Dictionary가 처리 후 어떻게 달라질지 직접 적어 보세요.




```

실제 구조와 비교:

```text
새 key가 추가되었는가?
status 값이 올바른가?
level 값이 올바른가?
원래 name, score, attendance는 유지되었는가?
```

---

## Step 6. Run & Compare

| 확인 항목 | 내 확인 결과 |
|---|---|
| 민수는 status가 수료인가? | |
| 지영은 점수 조건 때문에 보완 필요인가? | |
| 현우는 수료이면서 level이 우수인가? | |
| 서연은 출석 조건 때문에 보완 필요인가? | |
| 모든 학생 Dictionary에 status가 추가되었는가? | |
| 모든 학생 Dictionary에 level이 추가되었는가? | |

---

# Part 7. AND / OR 차이 생각하기

같은 요구사항이 다음처럼 바뀌었다고 가정합니다.

```text
출석률이 80 이상이거나 점수가 80 이상이면 특별 상담 대상에서 제외한다.
```

이전 조건과 무엇이 달라졌나요?

```text
이전: 출석률 >= 80 AND 점수 >= 80
변경: 출석률 >= 80 OR 점수 >= 80
```

### 아래 값을 다시 판단합니다.

| 출석률 | 점수 | AND 결과 | OR 결과 |
|---:|---:|---|---|
| 80 | 80 | | |
| 79 | 90 | | |
| 90 | 79 | | |
| 70 | 70 | | |

AND와 OR의 차이를 자신의 말로 설명하세요.

```text


```

---

# Part 8. 데이터 구조와 조건에서 자주 생기는 실수 찾기

완성 정답을 작성하지 말고, 먼저 문제점을 설명합니다.

## 실수 A — 주문마다 key 이름이 다름

```python
{"customer": "민수", "amount": 48000}
{"name": "지영", "price": 72000}
```

여러 데이터를 반복 처리할 때 어떤 문제가 생길까요?

```text


```

## 실수 B — 필요한 속성이 없음

무료배송 여부를 금액으로 판단해야 하는데 `amount`가 데이터에 없다면?

```text


```

## 실수 C — AND 대신 OR 사용

수료 기준이 `출석 >= 80 AND 점수 >= 80`인데 OR를 사용하면 어떤 학생이 잘못 수료될 수 있을까요?

```text


```

## 실수 D — 결과를 출력만 하고 저장하지 않음

이후 단계에서 `status`를 다시 사용해야 한다면 어떤 불편이 생길까요?

```text


```

---

# Part 9. Debugging Record

결과가 예상과 다르면 다음 순서로 확인합니다.

```text
내가 예상한 결과:

실제 결과:

문제가 데이터 구조에 있는가, 조건에 있는가?

필요한 key가 모두 존재하는가?

AND / OR를 올바르게 사용했는가?

경계값 80을 올바르게 포함했는가?

status와 level이 올바른 Dictionary에 저장되었는가?

내 가설:

이번에 한 군데만 수정할 부분:

수정 후 결과:
```

---

# Part 10. Python 공식 문서 — 105~112분

직접 분석과 구현을 충분히 진행한 뒤 문법이 기억나지 않을 때만 공식 문서를 확인합니다.

```text
무엇을 확인했나요?

왜 필요했나요?

문제 해결 생각은 이미 어디까지 끝냈나요?

문서 확인 후 어떤 부분만 수정했나요?
```

---

# Part 11. AI Review — 112~117분

내 데이터 설계와 구현을 끝낸 뒤 AI를 검증자로 사용합니다.

```text
내가 아래 문제의 데이터 구조와 복합 조건을 직접 설계하고 구현했습니다.
새 정답 코드를 작성하지 말고 다음만 검토해 주세요.

1. Dictionary에 빠진 필수 속성
2. 같은 종류의 데이터에서 key 구조가 일관적인지
3. AND / OR 조건을 잘못 해석한 부분
4. 경계값 80, 90에서 생길 수 있는 논리 오류
5. 계산 결과를 Dictionary에 저장하는 과정의 문제
6. 추가로 테스트해야 할 데이터

내 데이터 설계:
[여기에 붙여넣기]

내 조건 분석:
[여기에 붙여넣기]

내 코드:
[여기에 붙여넣기]
```

AI가 알려준 내용 중 내가 실제로 놓친 것:

```text


```

AI 의견 중 내가 다시 판단한 내용:

```text


```

---

# Part 12. Reflection — 117~120분

### 1. 현실의 한 대상을 Dictionary로 표현할 때 가장 먼저 생각할 것은?

```text


```

### 2. 같은 구조의 여러 데이터를 만드는 것이 왜 중요할까요?

```text


```

### 3. AND와 OR의 차이를 내 말로 설명하면?

```text


```

### 4. 계산한 결과를 기존 Dictionary에 저장하면 어떤 장점이 있을까요?

```text


```

---

# 오늘의 완료 체크

- [ ] 현실의 대상에서 필요한 속성을 스스로 골라낼 수 있다.
- [ ] 한 대상을 Dictionary로 설계할 수 있다.
- [ ] 같은 구조의 여러 대상을 List of Dictionaries로 만들 수 있다.
- [ ] key 이름을 일관되게 설계해야 하는 이유를 설명할 수 있다.
- [ ] 긴 요구사항에서 조건을 각각 분리할 수 있다.
- [ ] AND와 OR의 차이를 실제 데이터로 설명할 수 있다.
- [ ] 80, 90 같은 경계값을 실행 전에 확인했다.
- [ ] 계산 결과를 기존 Dictionary에 새 key로 저장할 수 있다.
- [ ] 예상과 실제 데이터 구조를 비교했다.
- [ ] AI는 데이터 설계와 구현을 끝낸 뒤 검증용으로 사용했다.

---

# 다음 회차 준비

다음 수업은 Chapter 09~10입니다.

수업 전에 다음 질문을 생각해 옵니다.

```text
1. 학생 처리 프로그램이 길어지면 어떤 기능들로 나눌 수 있을까?
2. 합격 여부 판단과 등급 판단은 같은 함수가 해야 할까, 나누는 것이 좋을까?
3. 함수가 일을 하려면 어떤 입력이 필요할까?
4. 함수가 만든 결과를 다음 코드가 사용하려면 무엇이 필요할까?
5. print와 return은 어떤 차이가 있을까?
```

> **데이터를 잘 표현하면 조건과 반복이 쉬워지고, 복잡한 요구사항은 조건을 하나씩 분해하면 해결할 수 있습니다.**