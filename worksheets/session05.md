# Session 05 Worksheet — 큰 문제를 작은 함수로 나누고 다시 연결하기

> **수업일:** 2026-09-22  
> **대상 Chapter:** Chapter 09~10  
> **수업 시간:** 120분

이 워크시트는 정답지가 아닙니다.

오늘은 `def` 문법을 많이 외우는 것이 목표가 아닙니다.

오늘의 핵심은 다음 두 단계입니다.

```text
큰 문제
→ 서로 다른 책임으로 나누기
→ 각 책임의 입력 / 처리 / 결과 정하기
→ 작은 함수로 만들기
→ return 값으로 다시 연결하기
→ 전체 프로그램 흐름 만들기
```

---

## 이름

- 이름: ______________________________
- 오늘 날짜: _________________________

---

# 오늘의 수업 규칙

오늘도 함수 코드부터 쓰지 않습니다.

```text
1. 큰 문제 안에 서로 다른 일이 몇 개인지 찾는다.
2. 각 일에 이름을 붙인다.
3. 한 함수가 한 가지 책임만 갖도록 나눈다.
4. 함수가 무엇을 입력받는지 정한다.
5. 함수가 무엇을 return해야 하는지 정한다.
6. 각 함수를 따로 테스트한다.
7. return 값을 변수에 받아 다음 단계로 전달한다.
8. 전체 데이터 흐름을 연결한다.
9. 실행 전에 결과를 예상한다.
10. 마지막에 AI로 함수 책임과 연결만 검증한다.
```

수업 초반에는 다음을 사용하지 않습니다.

- 생성형 AI
- 일반 웹 검색
- 블로그의 완성 코드 복사
- 다른 사람의 완성 함수

---

# Part 1. Memory Coding — 0~10분

다음 요구사항을 보고 **합격 여부를 판단하는 함수**를 아무 자료도 보지 않고 작성합니다.

```text
함수 이름: get_result
입력: score
80점 이상: "합격"
80점 미만: "불합격"
판단한 문자열을 함수 밖으로 돌려준다.
```

```python
# 아무 자료도 보지 않고 직접 작성하세요.







```

다음 호출 결과도 실행 전에 예상합니다.

| 호출 | 예상 결과 |
|---|---|
| `get_result(79)` | |
| `get_result(80)` | |
| `get_result(91)` | |

### 작성 후 체크

- [ ] 함수 이름을 의미 있게 작성했다.
- [ ] 함수가 필요한 입력을 매개변수로 받는다.
- [ ] 경계값 80을 정확히 처리한다.
- [ ] `print()`가 아니라 필요한 값을 `return`한다.
- [ ] 서로 다른 입력값으로 재사용할 수 있다.

내가 기억하지 못했거나 헷갈렸던 부분:

```text


```

---

# Part 2. Explain — 10~20분

### 1. `get_result(score)`에서 `score`는 왜 필요한가요?

```text


```

### 2. 이 함수의 책임을 한 문장으로 설명하면?

```text


```

### 3. 이 함수가 학생 이름까지 알아야 할까요? 이유는?

```text


```

### 4. `return "합격"`은 값을 어디로 보내는 것인가요?

```text


```

### 5. 함수가 너무 많은 일을 동시에 하면 어떤 점이 어려워질까요?

```text


```

---

# Part 3. Chapter 09 — 큰 문제를 작은 책임으로 나누기 — 20~45분

## 문제 A. 학생 성적 처리 프로그램 분해하기

다음 요구사항이 있습니다.

```text
학생을 한 명씩 확인한다.
80점 이상이면 합격, 아니면 불합격으로 판단한다.
90점 이상은 A, 80점 이상은 B, 그 외는 C 등급으로 판단한다.
학생 전체 평균을 계산한다.
학생별 이름, 합격 여부, 등급을 보기 좋게 출력한다.
마지막에 전체 평균을 출력한다.
```

**아직 함수를 작성하지 마세요.**

먼저 큰 문제 안의 서로 다른 책임을 찾아봅니다.

| 책임 후보 | 이 책임이 해야 하는 일 | 필요한 입력 | 만들어야 할 결과 |
|---|---|---|---|
| | | | |
| | | | |
| | | | |
| | | | |
| | | | |

---

## 함수 후보 이름 붙이기

다음 형태처럼 책임에 이름을 붙입니다.

```text
합격 여부 판단 → ______________________________
등급 판단 → __________________________________
평균 계산 → __________________________________
요약 문자열 만들기 → __________________________
```

좋은 함수 이름의 기준을 자신의 말로 적으세요.

```text


```

---

## 한 함수 = 한 책임인지 확인하기

아래 후보를 보고 한 함수가 너무 많은 일을 하는지 판단합니다.

```text
process_student_and_calculate_average_and_print_all_results(...)
```

이 함수가 맡고 있는 서로 다른 일은 무엇인가요?

```text
1.
2.
3.
4.
```

더 작은 책임으로 나누면 어떤 장점이 있을까요?

```text


```

---

## 함수 책임 카드 작성

### 함수 1

```text
함수 이름:
입력:
처리:
return 결과:
이 함수를 사용하는 다음 단계:
```

### 함수 2

```text
함수 이름:
입력:
처리:
return 결과:
이 함수를 사용하는 다음 단계:
```

### 함수 3

```text
함수 이름:
입력:
처리:
return 결과:
이 함수를 사용하는 다음 단계:
```

---

# Part 4. Predict & Test — 45~55분

이제 아래 두 함수를 직접 구현합니다.

```text
get_result(score)
get_grade(score)
```

완성 코드를 보지 않고 작성합니다.

```python
# get_result





# get_grade






```

실행 전에 경계값을 먼저 예상합니다.

| score | get_result 예상 | get_grade 예상 | 실제 결과 | 일치? |
|---:|---|---|---|---|
| 79 | | | | |
| 80 | | | | |
| 89 | | | | |
| 90 | | | | |
| 91 | | | | |

### 각 함수를 따로 테스트하는 이유는 무엇인가요?

```text


```

---

# 5분 Checkpoint — 55~60분

다음 문장을 완성합니다.

> 함수를 만들기 전에 먼저 ________________________________________________.

> 함수 하나에는 가능하면 ________________________________________________.

---

# Part 5. Chapter 10 — 작은 기능을 연결해 프로그램 만들기 — 60~80분

함수가 각각 잘 동작해도, 서로 연결되지 않으면 아직 하나의 프로그램이 아닙니다.

다음 함수들을 만든다고 가정합니다.

```text
get_result(score)
get_grade(score)
make_summary(name, result, grade)
```

---

## 입력 → 처리 → 결과 표

| 함수 | 입력 | 처리 | return 결과 | 결과를 사용하는 곳 |
|---|---|---|---|---|
| `get_result` | | | | |
| `get_grade` | | | | |
| `make_summary` | | | | |

---

## 데이터 흐름을 코드보다 먼저 그리기

아래 흐름의 빈칸을 채우세요.

```text
student
  ↓
name, score
  ├→ ____________________ → result ─┐
  └→ ____________________ → grade  ─┼→ ______________________________ → summary
name ────────────────────────────────┘
                                         ↓
                                     ____________________
```

### 핵심 질문

```text
get_result가 만든 result는 누가 사용하는가?
get_grade는 result를 입력받아야 하는가, score를 입력받아야 하는가?
make_summary가 일을 하려면 어떤 값들이 먼저 준비되어야 하는가?
summary를 사람에게 보여줄 마지막 단계는 무엇인가?
```

내 답:

```text


```

---

# Part 6. `print`와 `return` 직접 비교하기

다음 함수는 결과를 화면에 보여줍니다.

```python
def get_result(score):
    if score >= 80:
        print("합격")
    else:
        print("불합격")
```

다음 코드를 실행하면 `value`에는 무엇이 들어갈지 먼저 예상합니다.

```python
value = get_result(85)
print("value:", value)
```

내 예상:

```text


```

실제 실행 결과:

```text


```

### 왜 이런 결과가 나왔을까요?

```text


```

다음 두 문장을 완성하세요.

> `print`는 ________________________________________________.

> `return`은 _______________________________________________.

---

# Part 7. Main Practice — 80~105분

## 문제 B. 주문 처리 프로그램을 작은 함수로 설계하고 연결하기

다음 주문 데이터가 있습니다.

```python
orders = [
    {"order_id": 1001, "customer": "민수", "amount": 48000},
    {"order_id": 1002, "customer": "지영", "amount": 72000},
    {"order_id": 1003, "customer": "현우", "amount": 120000}
]
```

요구사항:

```text
1. 주문 금액이 50,000원 이상이면 배송비는 0원이다.
2. 50,000원 미만이면 배송비는 3,000원이다.
3. 주문 금액이 100,000원 이상이면 10,000원 할인한다.
4. 그 외에는 할인 금액이 0원이다.
5. 최종 결제 예정 금액 = 주문 금액 + 배송비 - 할인 금액
6. 주문번호, 고객명, 배송비, 할인 금액, 최종 금액을 요약 문자열로 만든다.
7. 주문을 하나씩 처리해 요약을 출력한다.
```

---

## Step 1. 큰 문제를 책임으로 나누기

| 필요한 기능 | 함수 이름 후보 | 입력 | return 결과 |
|---|---|---|---|
| 배송비 계산 | | | |
| 할인 계산 | | | |
| 최종 금액 계산 | | | |
| 요약 만들기 | | | |

---

## Step 2. 함수 사이 데이터 의존성 찾기

다음 질문에 답합니다.

```text
배송비 함수는 무엇만 알면 되는가?
할인 함수는 무엇만 알면 되는가?
최종 금액 함수가 일하려면 어떤 세 값이 필요한가?
요약 함수가 일하려면 어떤 정보가 준비되어야 하는가?
```

내 답:

```text


```

---

## Step 3. 한 주문만 먼저 손으로 추적하기

주문 1002를 먼저 처리합니다.

```python
{"order_id": 1002, "customer": "지영", "amount": 72000}
```

| 단계 | 예상 값 |
|---|---:|
| amount | 72,000 |
| shipping_fee | |
| discount | |
| final_amount | |
| summary | |

---

## Step 4. 함수별로 따로 구현하고 테스트하기

```python
# 배송비 함수




# 할인 함수




# 최종 금액 함수




# 요약 함수




```

각 함수가 혼자서 올바르게 동작했는지 확인:

- [ ] 배송비 함수 테스트 완료
- [ ] 할인 함수 테스트 완료
- [ ] 최종 금액 함수 테스트 완료
- [ ] 요약 함수 테스트 완료

---

## Step 5. 전체 데이터 흐름 연결하기

먼저 자연어 또는 의사코드로 적습니다.

```text
1.
2.
3.
4.
5.
6.
7.
8.
```

그 후 전체 프로그램을 연결합니다.

```python
orders = [
    {"order_id": 1001, "customer": "민수", "amount": 48000},
    {"order_id": 1002, "customer": "지영", "amount": 72000},
    {"order_id": 1003, "customer": "현우", "amount": 120000}
]

# 위에서 만든 함수들을 사용해 전체 흐름을 연결하세요.













```

---

## Step 6. Run & Compare

| 확인 항목 | 내 확인 결과 |
|---|---|
| 48,000원 주문에 배송비가 붙는가? | |
| 50,000원 경계에서 무료배송이 되는 구조인가? | |
| 120,000원 주문에 할인이 적용되는가? | |
| 최종 금액 계산에 amount, shipping_fee, discount가 모두 사용되는가? | |
| 각 함수의 return 값을 변수에 받아 다음 단계에서 사용하는가? | |
| 요약 함수가 필요한 입력을 모두 받는가? | |
| 반복문은 전체 흐름을 읽기 쉽게 보여주는가? | |

---

# Part 8. 함수 연결에서 자주 생기는 실수 찾기

완성 정답을 쓰지 말고 먼저 데이터 흐름이 어디에서 끊기는지 설명합니다.

## 실수 A — return 누락

```python
def get_discount(amount):
    if amount >= 100000:
        discount = 10000
    else:
        discount = 0
```

다음 코드에서 `discount` 변수에는 어떤 값이 들어갈까요?

```python
discount = get_discount(120000)
```

내 예상:

```text


```

---

## 실수 B — 반환값을 받지 않음

```python
get_shipping_fee(amount)
final_amount = amount + shipping_fee - discount
```

문제점:

```text


```

---

## 실수 C — 잘못된 값을 함수에 전달

```python
grade = get_grade(result)
```

`get_grade`가 점수를 필요로 하는 함수라면 무엇이 잘못되었나요?

```text


```

---

## 실수 D — 필요한 인자 누락

```python
summary = make_summary(name, result)
```

함수가 `name, result, grade` 세 값을 필요로 한다면 무엇을 먼저 확인해야 할까요?

```text


```

---

# Part 9. Debugging Record

함수 연결이 예상대로 동작하지 않으면 문법보다 먼저 **값의 흐름**을 추적합니다.

```text
내가 예상한 최종 결과:

실제 결과:

어느 함수까지는 정상인가?

문제가 생긴 함수의 입력값은 무엇이었는가?

그 함수가 실제로 return한 값은 무엇인가?

그 반환값을 어느 변수에 저장했는가?

다음 함수에 올바른 변수를 전달했는가?

None이 나온 곳이 있는가?

내 가설:

이번에 한 군데만 수정할 부분:

수정 후 결과:
```

---

# Part 10. Python 공식 문서 — 105~112분

직접 함수 책임과 연결을 설계한 뒤, 문법이 기억나지 않을 때만 공식 문서를 확인합니다.

```text
무엇을 확인했나요?

왜 필요했나요?

함수의 입력/출력 설계는 이미 끝냈나요?

문서 확인 후 어떤 문법만 수정했나요?
```

---

# Part 11. AI Review — 112~117분

내 함수 설계와 구현이 끝난 뒤 AI를 검증자로 사용합니다.

```text
내가 아래 프로그램을 직접 함수로 나누고 연결했습니다.
새 정답 코드를 작성하지 말고 다음만 검토해 주세요.

1. 한 함수가 너무 많은 책임을 맡고 있는 부분
2. 함수 이름과 실제 책임이 어긋나는 부분
3. 필요한 입력이 빠졌거나 불필요한 입력이 있는 부분
4. return 값이 다음 단계로 올바르게 전달되지 않는 부분
5. print와 return을 혼동한 부분
6. 데이터 흐름이 끊길 수 있는 위치
7. 추가로 독립 테스트해야 할 함수

내 함수 설계:
[여기에 붙여넣기]

내 데이터 흐름:
[여기에 붙여넣기]

내 코드:
[여기에 붙여넣기]
```

AI가 알려준 내용 중 내가 실제로 놓친 것:

```text


```

AI 의견 중 내가 다시 판단한 내용:

```text


```

---

# Part 12. Reflection — 117~120분

### 1. 큰 문제를 함수로 나눌 때 가장 먼저 찾아야 하는 것은?

```text


```

### 2. 함수의 입력과 return 결과를 먼저 정하면 어떤 점이 쉬워지나요?

```text


```

### 3. `print`와 `return`의 차이를 내 말로 설명하면?

```text


```

### 4. 여러 함수가 하나의 프로그램이 되려면 무엇이 필요할까요?

```text


```

---

# 오늘의 완료 체크

- [ ] 큰 문제 안의 서로 다른 책임을 먼저 찾았다.
- [ ] 한 함수가 한 가지 역할을 갖도록 나눌 수 있다.
- [ ] 함수 이름을 책임이 드러나도록 정할 수 있다.
- [ ] 함수를 작성하기 전에 입력·처리·return 결과를 설명할 수 있다.
- [ ] 함수를 전체 프로그램에 넣기 전에 따로 테스트했다.
- [ ] `print`와 `return`의 차이를 설명할 수 있다.
- [ ] return 값을 변수에 받아 다음 단계에서 사용할 수 있다.
- [ ] 함수 사이 데이터 흐름을 그림이나 의사코드로 먼저 그렸다.
- [ ] `None`이나 잘못된 인자가 생기면 값의 흐름부터 추적한다.
- [ ] AI는 함수 설계와 구현 후 검증용으로 사용했다.

---

# 다음 회차 준비

다음 수업은 마지막 Chapter 11~12입니다.

수업 전에 다음 질문을 생각해 옵니다.

```text
1. 오류가 발생했을 때 가장 먼저 무엇을 확인해야 할까?
2. 예상 결과와 실제 결과가 다르면 어디부터 추적해야 할까?
3. 오류 메시지는 정답일까, 단서일까?
4. 처음 보는 문제를 받았을 때 코드 전에 무엇을 적어야 할까?
5. AI 없이 먼저 시도해야 하는 순서를 말할 수 있을까?
```

> **함수는 `def` 문법이 아니라 책임을 나누는 도구이고, `return`은 작은 기능들을 연결하는 데이터 전달 장치입니다.**