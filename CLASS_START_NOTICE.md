# Python Foundation Bootcamp — 수업 시작 안내

안녕하세요.

이번 `Python Foundation Bootcamp`는 Python 문법을 처음부터 다시 배우는 수업이 아닙니다.
이미 배운 기본 문법을 이용해 **문제를 스스로 이해하고, 작은 단계로 나누고, 직접 구현하고, 오류를 수정하는 힘**을 훈련하는 과정입니다.

가장 중요한 목표는 다음 변화입니다.

```text
"어떤 Python 문법을 쓰지?"
             ↓
"이 문제는 무엇을 요구하고,
 어떤 데이터를 사용하고,
 무엇을 반복하고,
 무엇을 판단해야 하지?"
```

---

## 1. 첫 수업 전에 반드시 해 주세요

첫 수업은 **2026년 9월 8일**입니다.

수업 전에 아래 Chapter 00을 반드시 완료해 주세요.

- [Chapter 00. 시작하기 전에 — Python 기본 도구를 손에 익히기](blog/chapter00/chapter00.md)

Chapter 00을 마친 뒤에는 다음 코드를 **아무 자료도 보지 않고 직접 작성할 수 있도록** 준비합니다.

```python
students = [
    {"name": "민수", "score": 85},
    {"name": "지영", "score": 72},
    {"name": "현우", "score": 91}
]

for student in students:
    name = student["name"]
    score = student["score"]

    if score >= 80:
        result = "합격"
    else:
        result = "불합격"

    print(name, score, result)
```

단순히 외우는 것에서 끝나지 않습니다. 다음 질문에도 자신의 말로 답할 수 있어야 합니다.

```text
왜 학생 한 명은 Dictionary인가?
왜 여러 학생은 List에 들어 있는가?
for는 무엇을 반복하는가?
if는 무엇을 판단하는가?
80은 어떤 기준인가?
합격 기준을 70으로 바꾸면 무엇이 달라지는가?
```

### 첫 수업 전 준비 체크

```text
□ Chapter 00을 끝까지 학습했다.
□ 위 코드를 보지 않고 작성해 봤다.
□ 코드 각 부분의 역할을 설명할 수 있다.
□ 학생 한 명을 추가해 봤다.
□ 점수와 합격 기준을 바꿔 봤다.
□ 실행하기 전에 결과를 먼저 예상해 봤다.
□ Python 코드를 실행할 수 있는 환경을 준비했다.
```

---

## 2. 이 수업에서는 코드부터 쓰지 않습니다

문제를 받으면 바로 Python을 작성하기 전에 먼저 다음을 생각합니다.

```text
문제가 요구하는 것은 무엇인가?
입력은 무엇인가?
원하는 출력은 무엇인가?
어떤 데이터가 필요한가?
무엇이 반복되는가?
무엇을 판단해야 하는가?
어떤 순서로 처리해야 하는가?
```

그 다음에 Python 코드로 옮깁니다.

> **코드는 생각의 출발점이 아니라, 정리된 생각을 표현하는 도구입니다.**

---

## 3. 실행 버튼을 누르기 전에 먼저 예상합니다

수업 중에는 코드를 작성한 뒤 바로 실행하지 않습니다.

먼저 결과를 손으로 예상합니다.

```text
내 예상
→ 실행
→ 실제 결과
→ 비교
```

예상과 실제 결과가 다르면 그 차이가 바로 디버깅의 출발점이 됩니다.

---

## 4. 오류가 나도 바로 정답을 찾지 않습니다

오류가 발생하면 다음 순서로 확인합니다.

```text
예상
→ 실행
→ 차이 발견
→ 위치 찾기
→ 실제 값 확인
→ 원인 가설 하나
→ 한 부분만 수정
→ 재실행
→ 검증
```

오류 메시지는 실패가 아니라 **문제를 해결하기 위한 단서**입니다.

---

## 5. AI와 검색은 언제 사용하나요?

이 과정에서는 스스로 생각하는 시간을 확보하기 위해 문제 해결 초반에 다음을 사용하지 않습니다.

- 생성형 AI
- 일반 웹 검색
- 블로그의 완성 코드
- Stack Overflow의 완성 답안
- 다른 사람의 완성 코드

기본 순서는 다음과 같습니다.

```text
1. 내 머리로 먼저 생각한다.
2. 직접 문제를 분석한다.
3. 내가 알고 있는 Python으로 구현한다.
4. 실행 전에 결과를 예상한다.
5. 실행하고 오류를 직접 추적한다.
6. 필요한 경우 Python 공식 문서를 확인한다.
7. 마지막에 AI로 내 생각과 코드를 검증한다.
```

AI를 사용하지 않는 법을 배우는 과정은 아닙니다.

> **AI에게 생각을 맡기지 않고, 내가 먼저 생각한 뒤 AI를 검증자로 사용하는 방법을 연습합니다.**

---

## 6. 매 수업에서 사용할 학습 순서

매 회차는 거의 같은 흐름으로 진행합니다.

```text
Memory Coding
→ Explain
→ Think Before Code
→ Implement
→ Predict Before Run
→ Run & Compare
→ Debug by Hypothesis
→ Python Official Docs if Needed
→ AI Review
→ Reflection
```

이 순서를 반복해 **문제 해결 습관 자체를 만드는 것**이 이번 Bootcamp의 핵심입니다.

---

## 7. 학생용 워크시트를 함께 사용합니다

각 수업에서는 해당 Chapter 강의안과 함께 학생용 워크시트를 사용합니다.

워크시트는 정답지가 아닙니다.

다음 내용을 직접 기록하기 위한 자료입니다.

```text
문제 분석
입력 / 출력
조건 / 반복
처리 순서
실행 전 예상
직접 구현
오류 가설
수정 결과
회고
```

회차별 워크시트는 [README의 실습 워크시트](README.md#실습-워크시트)에서 바로 열 수 있습니다.

---

## 8. 전체 수업 일정

| 회차 | 수업일 | Chapter | 핵심 주제 |
|---|---|---|---|
| Session 01 | 2026-09-08 | Chapter 01~02 | 문제 구조 찾기 · 작은 단계로 분해 |
| Session 02 | 2026-09-10 | Chapter 03~04 | 판단 기준 · 반복되는 일 발견 |
| Session 03 | 2026-09-15 | Chapter 05~06 | 필터링 · 집계 |
| Session 04 | 2026-09-17 | Chapter 07~08 | 데이터 구조 · 복합 조건 |
| Session 05 | 2026-09-22 | Chapter 09~10 | 함수 책임 분리 · 기능 연결 |
| Session 06 | 2026-09-24 | Chapter 11~12 | 디버깅 · Final Challenge |

전체 학습 방법과 세부 일정은 다음 문서를 참고하세요.

- [Python Foundation Bootcamp — 학습 진행 가이드](COURSE_GUIDE.md)

---

## 9. 막혔을 때는 이렇게 도움을 요청합니다

단순히 `모르겠습니다`라고 하기보다 먼저 아래 내용을 정리합니다.

```text
1. 내가 이해한 문제는 무엇인가?
2. 내가 예상한 결과는 무엇인가?
3. 어디까지 직접 해봤는가?
4. 실제 결과는 무엇인가?
5. 정확히 어느 부분에서 막혔는가?
6. 내가 의심하는 원인은 무엇인가?
```

완벽한 답을 적을 필요는 없습니다.

**자신이 어디까지 생각했는지 말하는 것 자체가 문제 해결 연습의 일부입니다.**

---

## 10. 마지막으로 기억할 한 문장

> **외워서 시작하고, 생각해서 풀고, 직접 실행하고, 오류에서 배우고, 마지막에 AI로 검증합니다.**

첫 수업 전에는 무엇보다 **Chapter 00을 완료하고 기본 코드를 직접 작성해 보는 것**부터 준비해 주세요.
